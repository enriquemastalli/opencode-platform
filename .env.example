# ── Cloudflare Tunnel ──────────────────────────────────────────────────────────
# Token del tunnel creado en Cloudflare Zero Trust → Networks → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=eyJ...

# ── GitHub — creación automática de repositorios ───────────────────────────────
# Personal Access Token con permisos: repo, delete_repo
GITHUB_TOKEN=ghp_...
# Organización donde se crean los repos de los proyectos
GITHUB_ORG=essentia-uy

# ── Panel de gestión ───────────────────────────────────────────────────────────
# Puerto interno del panel (no se expone directamente, va por Traefik)
PANEL_PORT=3000

# Directorio donde se clonan los workspaces de proyectos en el VPS
WORKSPACES_DIR=/workspaces

# Puerto inicial para los contenedores de opencode (se asignan secuencialmente)
OPENCODE_BASE_PORT=4100

# Máximo de proyectos simultáneos
MAX_PROJECTS=20
